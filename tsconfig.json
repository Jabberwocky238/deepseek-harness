{
  // Solution file: the whole-repo graph for `tsc -b` and the tsserver entry.
  // `extends` carries the base paths for get-tsconfig consumers. `files: []`
  // keeps it program-less.
  "extends": "./tsconfig.base.json",
  "files": [],
  "references": [
    { "path": "./vendor" }
  ]
}
